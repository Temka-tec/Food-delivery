# food-delivery-v2
# Food-delivery2
# Food-delivery
# Food-delivery

## Deploy notes

- Frontend Vercel project: set `DATABASE_URL` to your production database connection string.
- The frontend now serves the Prisma-backed API routes itself under `/api`.
- The old backend folder can stay in the repo, but it is no longer required for the frontend deploy.
